import { Avatar, Badge, Space } from 'antd'

function Cartwidget() {
    return (
        <Badge count={2}>
            <Avatar shape="square" size="large" />
        </Badge>
    )
}

export default Cartwidget
